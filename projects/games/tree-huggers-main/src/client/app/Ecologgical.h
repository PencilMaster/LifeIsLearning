#pragma once
#include <wx/wx.h>

class Ecologgical : public wxApp {
	public:
		/** 
		@brief creates a game window and starts the game controller
		@return true if the above was successful
     	**/
		virtual bool OnInit();
};
